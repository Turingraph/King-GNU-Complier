# include "ascii.h"

// time : O(n)
// space: O(n)
char			*z_buffer_1d_line_id(t_ascii_canvas *canvas, t_2d_line **line_arr, size_t arr_len)
{
	return (NULL);
}

// time : O(n)
// space: O(n)
char			*z_buffer_1d(t_2d_line **line_arr, size_t arr_len, char is_sort)
{
	char	*output;

	if ((output = (char *)malloc(sizeof(char) * (1))) == NULL)
		return (NULL);
}
