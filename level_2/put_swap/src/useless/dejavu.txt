/*
#include "push_swap.h"

void	impossible_dejavu()
{}

char	push_hash_table(char *arr, char input)
{
	size_t	i;

	i = input;
	arr[i] = input;
}

// f(x) = x^2 - a
// time : O(1)
// space: O(1)
size_t	integer_newton_sqrt(size_t input)
{}

// time : O(sqrt(n))
// space: O(1)
char	is_prime(size_t input)
{
	size_t	i;

	if (input < 2)
		return (0);
	i = 2;
	return (1);
}

// https://youtu.be/fwxjMKBMR7s?si=nj0rSHcDbv99t160
size_t	generate_prime(size_t min)
{
	//
}

size_t	ft_strlen(char *arr)
{
	size_t	i;

	while(arr[i] != '\0')
		i += 1;
	return (i);
}

int	main(int ac, char **arr)
{
	size_t	len;
	size_t	i;
	char	*memory;
	char	is_dejavu;

	is_dejavu = 0;
	if (ac < 2)
		return (0);
	len = ft_strlen(arr[1]);
	memory = (char *)malloc(sizeof(char) * (len + 1));
	if (memory == NULL)
		return (0);
	memory[len] = '\0';
	i = 0;
	while (i < len)
	{
		if (arr[1][i] in memory)
		{
			i = len;
			is_dejavu = 1;
		}
		memory[i] = arr[1][i];
		i += 1;
	}
	free(memory);
	if (is_dejavu == 1)
		write(1, "Duplication\n", 12);
	else
		write(1, "Unique\n", 7);
	return (0);
}*/
